# Production Environment

# Enable Mock Data for demo (GitHub Pages is static, uses client-side mock interceptor)
VITE_USE_MOCK=true

# API Base URL (must include /api prefix for client-side mock interceptor to work)
VITE_API_BASE_URL=/api
